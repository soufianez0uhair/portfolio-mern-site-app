import YugiCardsLoader from '../assets/yugi-cards-loading.gif';

const Loader = () => {
    return (
        <img src={YugiCardsLoader} alt="yugi cards gif" className="loader" />
    )
}

export default Loader;